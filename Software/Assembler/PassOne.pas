var 
  currentAddress : cardinal;

{
    The primary purpose of this pass
    is to discover the location of labels,
    and install those labels
}
function PassOne(lex : pLexer; tok : Token) : boolean;
var
  output : boolean;
begin
  output := true;

  if 

  PassOne := output;
end;
